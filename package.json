{
  "name": "socket.io-file-client",
  "version": "3.0.0",
  "description": "Client module for Socket.io-file",
  "main": "socket.io-file-client.js",
  "scripts": {
    "start": "npm run build-prod;npm run serve",
    "build": "babel ./src -d ./build --copy-files",
    "build-dev": "NODE_ENV=development npm run build",
    "build-prod": "NODE_ENV=production npm run build",
    "serve": "node ./build/index.js",
    "flow": "flow",
    "dev": "npm run flow;babel ./src -d ./build --copy-files --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/rico345100/socket.io-file-client"
  },
  "keywords": [
    "WebSocket",
    "Socket.io",
    "binary",
    "arraybuffer",
    "file",
    "upload",
    "node.js"
  ],
  "author": ".modernator",
  "license": "MIT",
  "devDependencies": {
    "@babel/cli": "^7.2.3",
    "@babel/core": "^7.3.4",
    "@babel/preset-flow": "^7.0.0"
  }
}
